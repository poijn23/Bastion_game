<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <TargetFramework>net10.0</TargetFramework>
    <Nullable>enable</Nullable>
    <ImplicitUsings>enable</ImplicitUsings>
    <RootNamespace>Bastion.Contracts</RootNamespace>
    <AssemblyName>Bastion.Contracts</AssemblyName>
  </PropertyGroup>

  <ItemGroup>
    <!-- Misma version que System.ServiceModel.NetTcp del lado cliente (STACK.md, tabla
         del stack): si no coinciden, el canal no negocia. -->
    <PackageReference Include="System.ServiceModel.Primitives" Version="10.0.652802" />
  </ItemGroup>

</Project>
